[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
thiserror = "1.0.69"
types = { path = "../types" }

[dev-dependencies]
tempfile = "3"
